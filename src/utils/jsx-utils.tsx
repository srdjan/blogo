/**
 * Utility functions for working with JSX
 *
 * Note: This file is no longer needed as we're using the built-in `html` tag function from mono-jsx.
 * It's kept here for reference only.
 */

// The html tag function is now provided by mono-jsx
// No need to define our own version

// The RawHTML component is no longer needed
// Use the built-in html tag function instead:
// Example: {html`<div>Raw HTML content</div>`}
